# Hoist @tailwindcss packages so nested oxide can resolve native bindings (oxide-darwin-x64/arm64).
# Fixes: Cannot find native binding / Cannot find module '@tailwindcss/oxide-darwin-x64'
public-hoist-pattern[]=*tailwindcss*
